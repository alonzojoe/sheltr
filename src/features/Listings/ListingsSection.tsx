import Wrapper from "@/components/Containers/Wrapper";

const ListingsSection = () => {
  return (
    <div>
      <Wrapper className="py-2 px-3i">Listings</Wrapper>
    </div>
  );
};

export default ListingsSection;
